{
  "name": "lukeforge-threejs",
  "version": "1.0.0",
  "description": "Production-ready Three.js landing site for Lukeforge IT company",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint src --ext .js",
    "format": "prettier --write \"src/**/*.{js,css,html}\"",
    "serve:mock": "node server/mock-contact.js",
    "test:build": "npm run build && node -e \"console.log('✓ Build smoke test passed')\"",
    "deploy": "npm run build && netlify deploy --prod --dir=dist",
    "deploy-preview": "npm run build && netlify deploy --dir=dist"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "cors": "^2.8.5",
    "eslint": "^8.56.0",
    "express": "^4.18.2",
    "prettier": "^3.2.4",
    "terser": "5.39.0",
    "vite": "^5.0.11"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "threejs",
    "webgl",
    "vite",
    "landing-page",
    "3d-website"
  ],
  "author": "Lukeforge",
  "license": "MIT"
}
